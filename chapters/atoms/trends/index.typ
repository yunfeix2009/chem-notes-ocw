#import "/src/components/index.typ": docs-subchapter
#import "/lib.typ": *

#show: docs-subchapter.with(
  title: [Properties and Their Trends],
  route: "trends",
  children: [
    #include "ie/index.typ"
    #include "ea/index.typ"
    #include "en/index.typ"
  ],
)

1869 Dmitri Mendeleev (Russian, 1834-1907) introduced a periodic table based on
reoccurring physical properties and chemical properties of the elements. Some
examples of grouping by properties:
•	 Li, Na, and K were originally grouped together in a column because they are all
soft, malleable, reactive metals.
• He, Ne, and Ar were grouped together because of their inertness.

From knowledge of electron configurations and orbitals, categorizing elements by their $n$ and further by their valence electron configuration is motivated.
More physical, expressed properties of elements are summarized by periodic trends, as typical in chemistry, and come with exceptions, as typical in chemistry.

Each row in the periodic table is called a period, with same $n$, while each column is called a group (with the same valence electron configuration, with the exception of $"He"$ being with all the full elements).

This section examines properties of atoms and qualitative predictions of of relative values of elements from abstract rules.
